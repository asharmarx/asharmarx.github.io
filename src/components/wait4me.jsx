import { Wait4MeWrap } from "../styles/myStyles";

const Wait4Me = () => (
    <Wait4MeWrap>
        <span>
        <h1>Wait for me....</h1>
        </span>
    </Wait4MeWrap>)

export default Wait4Me;